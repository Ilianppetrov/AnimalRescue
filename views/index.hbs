{{#each animals}}
<div class="row">
   {{current}}
    {{#each this}}
    <div class="col-xs-12 col-md-4">  
      <div class="thumbnail clearfix hoover">
        <div>
          <a href="/animal/profile/{{this.id}}" class="index-image-containter">
            <img class="img-responsive index-image" src="/{{this.imagePath}}" alt="...">
          </a>
        </div>
        <p class="animal-name pull-left">{{this.name}}</p>
        <p class="animal-age pull-right">{{this.months}} months</p>
        <p class="animal-age pull-right">{{this.years}} years</p>
      </div>
    </div>
    {{/each}}
</div>
{{/each}}
<div class="row">
  <div class="col-md-3 col-md-offset-2 col-xs-2 col-xs-offset-2"></div>
  <nav aria-label="Page navigation">
    <ul class="pagination pagination-lg">
      {{# if previous}}
      <li><a href="{{previousValue}}" aria-label="Previous">
          <span aria-hidden="true">&laquo;</span>
        </a></li>
      {{/if}}
      {{# each pages}}
      <li><a href="{{this.url}}">{{this.number}}</a></li>
      {{/each}}
      {{# if next}}
      <li><a href="{{nextValue}}" aria-label="Next">
          <span aria-hidden="true">&raquo;</span>
        </a></li>
      {{/if}}
    </ul>
  </nav>
</div>
</div>